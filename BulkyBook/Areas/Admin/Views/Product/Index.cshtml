

@model List<Product>

<div class="shadow border-0 p-3 mt-2">



    <div class="row mb-3">
        <div class="col-6">
            <h1 class="text-center text-light">Product List</h1>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary"><i class="bi bi-plus-circle-fill"></i> Create New Product</a>
        </div>
    </div>

    <table class="table table-striped table-bordered" id="myTable" width="100%">
        <thead>
            <tr class="text-center">
                <th class="p-3 text-light">Title</th>
                <th class="p-3 text-light">ISBN</th>
                <th class="p-3 text-light">Price</th>
                <th class="p-3 text-light">Auther</th>
                <th class="p-3 text-light">Category</th>
                <th class="p-3 text-light">Action</th>
            </tr>
        </thead>
        <tbody>
            @*@foreach (var item in Model)
                {
                    <tr class="text-center">
                        <td>@(item.Title)</td>
                        <td>@item.ISBN</td>
                        <td>@item.Price</td>
                        <td>@item.Auther</td>
                        <td>@item.Catgories.CategoryName</td>
                        <td>

                            <a asp-controller="Product" asp-action="Upsert" asp-route-id="@item.Id" class="btn btn-md btn-primary"><i class="bi bi-pencil-square"></i> Update</a>
                            <a asp-controller="Product" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-md btn-danger"><i class="bi bi-trash-fill"></i> Delete</a>

                        </td>
                    </tr>
                }*@
        </tbody>
    </table>

</div>


@section Scripts{
    <script src="~/js/product.js"></script>
}